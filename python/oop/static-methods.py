# A static method in Python is a method inside a class that does not depend on the instance (self) or the class (cls).
# It behaves just like a normal function, but it belongs to a class for logical grouping.

# We define it using the @staticmethod decorator.